<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F57C00">

    <!-- A TextView Component for success ticket's reservation -->
    <TextView
        android:id="@+id/successMessage"
        android:layout_width="400dp"
        android:layout_height="70dp"
        android:layout_marginStart="176dp"
        android:layout_marginTop="270dp"
        android:layout_marginEnd="177dp"
        android:autoSizeTextType="uniform"
        android:text="Your ticket reservation has been successfully registered."
        android:textAlignment="center"
        android:textColor="#ffffff"
        android:textSize="17sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- A success-check icon. -->
    <ImageView
        android:id="@+id/successIcon"
        android:layout_width="175dp"
        android:layout_height="175dp"
        android:layout_marginStart="118dp"
        android:layout_marginTop="50dp"
        android:layout_marginEnd="118dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/check_button_foreground" />

    <!-- A textView to inform for payment email -->
    <TextView
        android:id="@+id/paymentMessage"
        android:layout_width="350dp"
        android:layout_height="70dp"
        android:layout_marginStart="25dp"
        android:layout_marginTop="82dp"
        android:layout_marginEnd="25dp"
        android:autoSizeTextType="uniform"
        android:text="To complete your booking you will be sent an e-mail, where you can proceed with the payment of your tickets"
        android:textAlignment="center"
        android:textColor="@color/white"
        android:textSize="15dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/successMessage" />





</androidx.constraintlayout.widget.ConstraintLayout>